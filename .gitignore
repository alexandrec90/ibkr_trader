# Secrets & local config
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# The devkit checkout the pr-gate workflow lands beside this one to drift-check
# against. CI-only, but ignored here too so a local `sync-devkit.py --src .devkit-src`
# never shows up as untracked noise.
.devkit-src/

# Data & runtime artifacts
data/
artifacts/
.local/
models/
mlruns/
reports/
*.parquet
*.csv
!tests/fixtures/*.csv
logs/
*.log

# IB Gateway / IBC local state
ibc/
tws_settings/

# OS / editor
.DS_Store
Thumbs.db
